# Copy to .env for local development. Do not commit .env.
# On Hugging Face Spaces, set these as Repository secrets instead.

OPENAI_API_KEY=

# Optional — email is skipped unless all three are set
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=
