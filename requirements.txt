# Hugging Face Space dependencies (direct deps only).
# Gradio is installed by the Space runtime as gradio[oauth,mcp]==6.14.0 — do not list it here.
# pydantic<=2.12.5 is required by gradio[mcp]; see pyproject.toml.
openai-agents
huggingface_hub>=0.20,<1.0
python-dotenv
pydantic>=2.11.10,<=2.12.5
pydantic-settings
sendgrid
